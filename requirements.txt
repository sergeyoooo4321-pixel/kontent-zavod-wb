fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.4
tenacity>=9.0
boto3>=1.35
aiobotocore>=2.13
openpyxl>=3.1
pillow>=10.4
aiofiles>=24.1
python-multipart>=0.0.9
# ── Hybrid pipeline (refactor_plan.md §4) ─────────────
# rembg: U2Net фон-ремовер локально (~170MB модели при первом запуске)
#   ВАЖНО: ставим именно [cpu] extra — иначе onnxruntime не подтянется и
#   `from rembg import remove` упадёт на SystemExit ещё на импорте.
# playwright: headless chromium для рендера плашек из HTML/CSS
# jinja2: шаблонизатор плашек
rembg[cpu]>=2.0.59
playwright>=1.47
jinja2>=3.1
pytest>=8.3
pytest-asyncio>=0.24
pytest-httpx>=0.30
respx>=0.21
